[tool.poetry]
name = "intro-pytest"
version = "0.1.0"
description = ""
authors = ["sol"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pytest = "^8.2.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
